const { startBox } = require("../systems/boxes");

module.exports = {
    name: "testbox",
    run: async (msg) => {

        startBox(msg.channel);

        msg.reply("📦 Test box aktivizēts!");
    }
};