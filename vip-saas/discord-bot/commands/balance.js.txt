const { getUser } = require("../database.js");

module.exports = {
    name: "!balance",

    execute(message) {

        getUser(message.author.id, (data) => {
            message.reply(`💰 Coins: ${data.coins} | 💎 Diamonds: ${data.diamonds}`);
        });
    }
};