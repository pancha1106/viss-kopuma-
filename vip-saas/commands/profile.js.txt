const { getUser } = require("../systems/economy");

module.exports = {
    name: "profile",
    run: async (msg) => {

        const u = getUser(msg.author.id);

        msg.channel.send(`
👤 PROFILE

🪙 Coins: ${u.coins}
💎 Diamonds: ${u.diamonds}
⭐ XP: ${u.xp}
👑 VIP: ${u.vip}
        `);
    }
};